To set up dotfiles (ssh, aliases, git-config etc.):
```shell
git clone git@github.com:essoen/dotfiles.git
cd dotfiles
sh setup.sh
```

To install software:

```shell
cd software
sh nonsudo-setup.sh
sudo sh sudo-setup.sh
```
